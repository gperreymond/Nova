machine:
  timezone:
    Europe/Paris
  node:
    version: 8
  services:
    - docker

dependencies:
  pre:
    - npm uninstall -g npm
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  override:
    - yarn install

test:
  override:
    - make tests
